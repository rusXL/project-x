apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: api
spec:
  type: NodePort
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
