apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: agama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: rusxl/agama-frontend:0becbfbee3fbd609059ea54d973591efd4edd0f4
          ports:
            - name: http
              containerPort: 3000
          env:
            # TODO: Secret/ConfigMap later
            - name: PORT
              value: "3000"
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: API_URL
              value: "http://api"
          # TODO: resources, probes
